Git eclipse 사용법

1.github에 Repository를 생성한다.

2.project -> team -> share
나의 local Repository를 생성한다.

3.commit
commit은 나의 local Repository에 올리는 것을 말한다.

4. push
github(원격 저장소)에 올리는 것을 말한다.
처음에 생성후에 올릴때는 , master로 올린다.

5. new branch
master는 github와 sync를 맞추는 곳이기 때문에,
작업해서는 안된다.
그래서 new branch를 통해 다른 branch로 작업한다.
new branch를 하면 이전 작업 내용이 없기때문에, 올릴려고 해도 올라가지 않는다.
또한 처음 new branch 분기시에 master의 내용을 merge하여 sync를 맞춰야 한다.

5. commit
새로운 branch에서 작업후, branch의 저장소를 생성한다. 새로운 저장소에 작업 내용을 올린다.

6.push
branch로 push를 한다.
그럼 github에서는 새로운 branch가 push하여 분기한
내용이 표시된다.

7.merge
master로 checkout하여 merge를 한다.

8.show history
show hisoty를 통해 어떤 branch가 언제 commit을 했는지 알수있다.